% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_multipartite}
\alias{plot_multipartite}
\title{Plot multi-partite graph using base R graphics
Draws a multi-layer LASAGNA graph as a parallel coordinate-style
layout using base R \code{igraph::plot}.}
\usage{
plot_multipartite(
  graph,
  layers = NULL,
  min.rho = 0.5,
  ntop = 25,
  labels = NULL,
  cex.label = 1,
  vx.cex = 1,
  xpos = NULL,
  xlim = NULL,
  justgraph = FALSE,
  edge.cex = 1,
  edge.alpha = 0.33,
  edge.gamma = 2,
  edge.colors = c("blue3", "magenta4"),
  xdist = 1,
  normalize.edges = FALSE,
  yheight = 0.85,
  edge.sign = c("both", "pos", "neg", "consensus")[1],
  edge.type = c("both", "inter", "intra", "both2")[1],
  labpos = NULL,
  value.name = NULL,
  strip.prefix = FALSE,
  strip.prefix2 = FALSE,
  prune = FALSE,
  do.plot = TRUE,
  color.var = "value",
  layout = c("parallel", "hive")[1]
)
}
\arguments{
\item{graph}{An igraph object (output of
\code{solve}).}

\item{layers}{Character vector of layers to include.}

\item{min.rho}{Minimum absolute edge weight.}

\item{ntop}{Number of top features per layer.}

\item{labels}{Optional named character vector of node labels.}

\item{cex.label}{Label size multiplier.}

\item{vx.cex}{Vertex size multiplier.}

\item{xpos}{Numeric vector of x-positions for layers.}

\item{xlim}{X-axis limits.}

\item{justgraph}{Only plot graph (no labels/titles).}

\item{edge.cex}{Edge width multiplier.}

\item{edge.alpha}{Edge transparency.}

\item{edge.gamma}{Gamma exponent applied to scaled edge widths.}

\item{edge.colors}{Two colors for negative and positive edge weights.}

\item{xdist}{Distance between layers.}

\item{normalize.edges}{Normalize edges per connection type.}

\item{yheight}{Height of the y-axis layout.}

\item{edge.sign}{Edge sign filter.}

\item{edge.type}{Edge type filter.}

\item{labpos}{Label position (2=left, 4=right).}

\item{value.name}{Display name for node values.}

\item{strip.prefix}{Remove layer prefix from labels.}

\item{strip.prefix2}{Remove prefix with regex.}

\item{prune}{Remove disconnected vertices.}

\item{do.plot}{Logical; if \code{FALSE}, compute the layout without drawing.}

\item{color.var}{Vertex colouring: \code{"value"} (by sign) or
\code{"color"} (use the vertex \code{color} attribute).}

\item{layout}{Layout type: \code{"parallel"} or \code{"hive"}.}
}
\value{
Invisibly, a list with the pruned \code{graph} and its
  \code{layout} coordinate matrix. Called mainly for the plot side effect.
}
\description{
Plot multi-partite graph using base R graphics
Draws a multi-layer LASAGNA graph as a parallel coordinate-style
layout using base R \code{igraph::plot}.
}
\examples{
set.seed(1)
gx <- matrix(rnorm(20 * 10), 20, 10,
  dimnames = list(paste0("g", 1:20), paste0("S", 1:10)))
px <- matrix(rnorm(15 * 10), 15, 10,
  dimnames = list(paste0("p", 1:15), paste0("S", 1:10)))
samples <- data.frame(group = rep(c("A", "B"), each = 5),
  row.names = paste0("S", 1:10))
data <- list(X = list(gx = gx, px = px), samples = samples)
model <- create_model(data, ntop = 10, nc = 5)
g <- solve(model, pheno = colnames(model$Y)[1], prune = FALSE)
mp <- plot_multipartite(g, min.rho = 0, ntop = 10)
}
